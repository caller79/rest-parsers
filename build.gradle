buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
    }
    ext {
        spotbugsVersion = '1.6.9'
    }
}

plugins {
    id 'com.github.spotbugs' version '4.0.0' apply false
    id 'maven-publish'
}

apply from: "$rootProject.projectDir/gradle/tasks.gradle"
